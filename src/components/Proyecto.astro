---
// Definir los props que el componente aceptará
export interface Props {
  title: string;
  description: string;
  imageUrl: string; // Nueva prop para la URL de la imagen
}

const { title, description, imageUrl } = Astro.props;
---
<a href="https://www.youtube.com/watch?v=dQw4w9WgXcQ&pp=ygUJcmlja3JvcGxs">
  <div class="p-20 shadow rounded h-200" style={`background-image: url(${imageUrl}); background-size: cover; background-position: center;`}>

  </div>
  
</a>
